export default class HolbertonClass {
  constuctor(size, name) {
    this.size = size;
    this.name = name;
  }

  valueOf() {
    return this._size;
  }

  toString() {
    return this._location;
  }
}
